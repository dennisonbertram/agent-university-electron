/**
 * STUB — implemented in GREEN commit.
 *
 * In GREEN this will use contextBridge.exposeInMainWorld('api', { ... }).
 * For RED we intentionally export nothing so the renderer cannot announce
 * itself and BT-L1-2 / BT-L1-4 will fail.
 */
export {}
