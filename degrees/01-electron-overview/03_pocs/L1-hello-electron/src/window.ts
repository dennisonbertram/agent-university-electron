/**
 * STUB — implemented in GREEN commit.
 * createMainWindow is responsible for creating the single L1 BrowserWindow
 * with secure defaults and loading the renderer index.html.
 */
import type { BrowserWindow } from 'electron'

export function createMainWindow(): BrowserWindow {
  throw new Error('createMainWindow: not implemented (stub for RED commit)')
}
